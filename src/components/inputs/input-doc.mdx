<!-- Input Doc -->
## Input 输入框
通过鼠标或键盘输入内容，是最基础的表单域的包装。

import { Story, Meta, ArgsTable, Source } from '@storybook/addon-docs/blocks';
import { Input } from './input';
import dedent from 'ts-dedent';
<Meta title="Components/Input" component={Input} />;

### 使用

<Source
  language='jsx'
  code={
    dedent`
      <Input
        size={size}
        placeholder={placeholder}
        disabled = {disabled}
        prepend={prepend}
        append={append}
        defaultValue={defaultValue}
        onChange={action('changed')}
        style={{ width: 300  }}
      />
    `
  }
/>

### 具体参数
<ArgsTable of={Input} />

export const Template = (args) => <Input {...args} />


### 组件展示


##### default 类型

<Story name="default" id="input--default">
{Template.bind({})}
</Story>

##### 前置标签

<Story name="default" id="input--with-prepend">
{Template.bind({})}
</Story>

##### 后置标签

<Story name="append" id="input--width-append">
{Template.bind({})}
</Story>

##### 带有默认值

<Story name="prepend" id="input--default-value">
{Template.bind({})}
</Story>